@model List<TankDetailsViewModel>


@foreach (var tank in Model)
{
    using (Html.BeginCollectionItem("TankFrequency.Tanks"))
    {
        Html.RenderPartial("TankRentalInvoice/_PartialInvoiceTankDetails", tank);
    }
}