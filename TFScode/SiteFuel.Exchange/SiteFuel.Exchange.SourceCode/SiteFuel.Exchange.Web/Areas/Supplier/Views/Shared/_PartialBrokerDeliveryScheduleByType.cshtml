@model List<DeliveryScheduleViewModel>

@if (Model != null && Model.Count() > 0)
{
    foreach (var deliverySchedule in Model)
    {
        Html.RenderPartial("_PartialBrokerDeliverySchedule", deliverySchedule);
    }
}


