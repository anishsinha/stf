@section header{
    <base href="/">
}
@{
    ViewBag.Title = "Value Mapping";
}
@using (Html.BeginScripts())
{
    <script>
        var selectedCountryAndCurrency = getSelectedCountryAndCurrency();
        var currentCompanyId = '@CurrentUser.CompanyId';
        var defaultCountryId = selectedCountryAndCurrency.countryId;
        var carrierMappingVisible = false;
        var IsLiftFileValidationEnabled = @ViewBag.IsLiftFileValidationEnabled;
        if('@CurrentUser.IsBuyer' == 'True'|| '@CurrentUser.IsBuyerAdmin' == 'True' || '@CurrentUser.IsBuyerCompany' == 'True')
        {
            carrierMappingVisible = true;
        }
    </script>
    Html.RenderPartial("~/Views/Shared/_PartialAngularBundle.cshtml");
}